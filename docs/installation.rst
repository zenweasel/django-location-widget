============
Installation
============

At the command line::

    $ easy_install django-location-widget

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-location-widget
    $ pip install django-location-widget